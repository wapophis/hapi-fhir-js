import CodeableConceptDt from './CodeablieConceptDt.js';
import {isValid} from '../../utils/ValidationRules.js';

export default class BoundCodeableConceptDt extends CodeableConceptDt{
    constructor(enumerable){
        if(isValid(enumerable)){
            
        }
    }
}